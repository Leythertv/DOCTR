# DOCTR - OCR Pipeline Dependencies
# Core OCR library
python-doctr[torch]>=0.8.0

# HTTP requests for Ollama API
requests>=2.28.0

# Image processing (often required by doctr)
Pillow>=9.0.0

# Additional dependencies that may be needed
numpy>=1.21.0
opencv-python>=4.5.0

# Optional: For better performance
# torch>=1.12.0  # Usually installed with python-doctr[torch]
# torchvision>=0.13.0  # Usually installed with python-doctr[torch]